version: '3.7'

services:
  watcher:
    build:
      target: production
    command: npm run start
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - TZ=America/Denver